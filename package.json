{
  "name": "dig-deep-descent",
  "version": "0.3.0",
  "description": "An offline Canvas sci-fi mining and exploration game.",
  "main": "js/main.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "build": "node scripts/build.js",
    "test": "jest --runInBand",
    "test:browser": "playwright test",
    "check": "npm run build && npm test && npm run test:browser",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@babel/core": "^7.25.2",
    "@babel/preset-env": "^7.25.4",
    "@playwright/test": "^1.63.0",
    "babel-jest": "^29.7.0",
    "esbuild": "^0.28.2",
    "jest": "^29.7.0"
  },
  "dependencies": {
    "jest-environment-jsdom": "^29.7.0"
  }
}
